---
phase: 02-safe-enrollment-lifecycle-recovery
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/src/inventory.rs
autonomous: true
requirements:
  - FAN-04
gap_closure: true
must_haves:
  truths:
    - "User can inspect a fan channel and see every safe control mode discovery actually found, not just PWM."
    - "User can choose voltage when hardware safely exposes it, while PWM-only hardware still reports PWM-only." 
    - "DBus-backed CLI enrollment choices reflect real discovered modes rather than accepting modes the inventory never advertised."
  artifacts:
    - path: crates/core/src/inventory.rs
      provides: "Live multi-mode fan capability discovery including safe non-PWM modes"
      contains: "ControlMode::Voltage"
  key_links:
    - from: crates/core/src/inventory.rs
      to: crates/daemon/src/main.rs
      via: Inventory snapshot fan.control_modes
      pattern: control_modes
    - from: crates/core/src/inventory.rs
      to: crates/cli/src/main.rs
      via: inventory/state output and enrollment expectations
      pattern: ControlMode::Voltage
---

<objective>
Close the Phase 2 control-mode selection gap by teaching live inventory discovery to surface all safe writable control modes it can actually prove on hardware.

Purpose: FAN-04 is still blocked because the daemon and CLI nominally accept `voltage`, but discovery only reports PWM today.
Output: Real multi-mode capability discovery plus regression coverage proving inventory surfaces PWM-only vs multi-mode hardware correctly.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-safe-enrollment-lifecycle-recovery/02-CONTEXT.md
@.planning/phases/02-safe-enrollment-lifecycle-recovery/02-RESEARCH.md
@.planning/phases/02-safe-enrollment-lifecycle-recovery/02-VERIFICATION.md
@.planning/phases/02-safe-enrollment-lifecycle-recovery/02-01-SUMMARY.md

<interfaces>
From crates/core/src/inventory.rs:
```rust
pub enum ControlMode {
    Pwm,
    Voltage,
}

pub struct FanChannel {
    pub id: String,
    pub channel: u32,
    pub rpm_feedback: bool,
    pub current_rpm: Option<u64>,
    pub control_modes: Vec<ControlMode>,
    pub support_state: SupportState,
    pub support_reason: Option<String>,
}
```

Current gap location:
```rust
let control_modes = if pwm_writable {
    vec![ControlMode::Pwm]
} else {
    Vec::new()
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add real multi-mode discovery rules</name>
  <files>crates/core/src/inventory.rs</files>
  <behavior>
    - Test 1: discovery returns `[Pwm, Voltage]` or equivalent stable ordering when fixture hardware exposes both safe writable control paths.
    - Test 2: discovery returns only `[Pwm]` for PWM-only writable hardware.
    - Test 3: discovery does not advertise `Voltage` when the node exists but is not writable or cannot be proven safe.
  </behavior>
  <action>Extend live fan capability discovery in `crates/core/src/inventory.rs` so `FanChannel.control_modes` reflects every safe writable mode the kernel hwmon/sysfs surface actually exposes, including `ControlMode::Voltage` when present, per FAN-04 and D-13. Reuse the existing support classification path instead of adding CLI-side guesses; unsupported or ambiguous hardware must remain `partial`/`unavailable` with an explanatory `support_reason`, not be silently promoted. Keep PWM-only hardware behavior unchanged, and document any detection heuristic inline so later control-loop work can trust it.</action>
  <verify>
    <automated>cargo test -p kde-fan-control-core inventory -- --nocapture</automated>
  </verify>
  <done>`build_fan_channel()` (or extracted helpers) can produce multiple discovered control modes, and tests cover safe voltage, PWM-only, and non-writable cases.</done>
</task>

<task type="auto">
  <name>Task 2: Prove downstream surfaces now reflect discovered modes</name>
  <files>crates/core/src/inventory.rs</files>
  <action>Add or extend focused regression tests in the same crate that serialize discovered fan channels through the existing inventory snapshot shape so downstream DBus/CLI consumers receive the real `control_modes` list without additional wiring changes. The proof here is that Phase 2's existing daemon/CLI contract can already carry the richer inventory data once discovery emits it; avoid touching CLI/daemon files unless tests show the core snapshot contract is insufficient.</action>
  <verify>
    <automated>cargo test -p kde-fan-control-core inventory::tests -- --nocapture</automated>
  </verify>
  <done>Regression coverage shows discovered control modes flow through the inventory snapshot model and Phase 2 no longer advertises only PWM by construction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sysfs hwmon → daemon discovery | Kernel-exposed hardware attributes may be incomplete, non-writable, or board-specific |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | T | `crates/core/src/inventory.rs` mode detection | mitigate | Only advertise `Voltage` when a concrete writable sysfs control path is present; otherwise keep support partial/unavailable with explicit reason |
| T-02-05-02 | I | inventory snapshot / CLI mode display | mitigate | Drive all downstream consumers from `FanChannel.control_modes` so user-visible mode choices come from one authoritative source |
| T-02-05-03 | D | ambiguous hardware capability exposure | accept | Some boards may expose PWM only; acceptable when discovery reports that limitation explicitly rather than fabricating alternate modes |
</threat_model>

<verification>
- `cargo test -p kde-fan-control-core inventory -- --nocapture`
- `cargo test --workspace`
</verification>

<success_criteria>
- Verification gap for control-mode choice is closed with real multi-mode discovery support.
- Inventory now reports voltage support only when live hardware exposure proves it.
- Existing DBus/CLI surfaces can consume the richer mode list without inventing client-side capability logic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-safe-enrollment-lifecycle-recovery/02-05-SUMMARY.md`
</output>
